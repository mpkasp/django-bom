from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'supersecretkey'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = ['*']

BOM_CONFIG = {
    'admin_dashboard': {
        'enable_autocomplete': False,
        'page_size': 50,
    },
    # Live-sourcing providers exposed in this deployment. Omit for "all providers"; set an
    # allowlist to feature-flag one off until it's ready (e.g. ['mouser'] to hide Nexar).
    'sourcing_providers_enabled': ['mouser'],
}

# Encryption key(s) for sourcing credentials stored on Organization (Fernet).
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Keep this SEPARATE from SECRET_KEY. A list enables key rotation (new key first).
# Only required if an organization actually stores sourcing credentials.
BOM_SOURCING_ENCRYPTION_KEYS = ['REPLACE_WITH_A_FERNET_KEY']

# google GoogleOAuth
SOCIAL_AUTH_GOOGLE_OAUTH2_KEY = 'secretkey'
SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET = 'secretkey'

# Database
# https://docs.djangoproject.com/en/1.10/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

FIXER_ACCESS_KEY = 'secretkey from fixer.io' # for exchange rate conversions